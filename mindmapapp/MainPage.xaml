<Page
    x:Class="mindmapapp.MainPage"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mindmapapp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mm="using:mindmapapp.Controls"
    mc:Ignorable="d">

    <Canvas Background="#FFEDEDED">
        <mm:Thought HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="tghtMaster" Canvas.Left="{Binding Point.X}" Canvas.Top="{Binding Point.Y}"/>

        <mm:Thought HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="tghtParent1" Canvas.Left="{Binding Point.X}" Canvas.Top="{Binding Point.Y}"/>
        <mm:Thought HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="tghtParent2" Canvas.Left="{Binding Point.X}" Canvas.Top="{Binding Point.Y}"/>

        <mm:Thought HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="tghtChild1" Canvas.Left="{Binding Point.X}" Canvas.Top="{Binding Point.Y}"/>
        <mm:Thought HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="tghtChild2" Canvas.Left="{Binding Point.X}" Canvas.Top="{Binding Point.Y}"/>
        <mm:Thought HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="tghtChild3" Canvas.Left="{Binding Point.X}" Canvas.Top="{Binding Point.Y}"/>
    </Canvas>
</Page>
